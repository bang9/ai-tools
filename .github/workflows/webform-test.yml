name: "webform: Test"

on:
  push:
    branches: [main]
    paths:
      - 'webform/**'
      - 'shared/upgrade/**'
  pull_request:
    branches: [main]
    paths:
      - 'webform/**'
      - 'shared/upgrade/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: webform/go.mod
          cache-dependency-path: webform/go.sum

      - name: Run tests
        run: |
          cd webform
          go test ./...
