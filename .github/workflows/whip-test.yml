name: "whip: Test"

on:
  push:
    branches: [main]
    paths:
      - 'whip/**'
      - 'shared/upgrade/**'
  pull_request:
    branches: [main]
    paths:
      - 'whip/**'
      - 'shared/upgrade/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: whip/go.mod
          cache-dependency-path: whip/go.sum

      - name: Run tests
        run: |
          cd whip
          go test ./...
