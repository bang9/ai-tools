name: "vaultkey: Test"

on:
  push:
    branches: [main]
    paths:
      - 'vaultkey/**'
      - 'shared/upgrade/**'
  pull_request:
    branches: [main]
    paths:
      - 'vaultkey/**'
      - 'shared/upgrade/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: vaultkey/go.mod
          cache-dependency-path: vaultkey/go.sum

      - name: Run tests
        run: |
          cd vaultkey
          go test ./...
