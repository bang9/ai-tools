name: "memex: Test"

on:
  push:
    branches: [main]
    paths:
      - 'memex/**'
  pull_request:
    branches: [main]
    paths:
      - 'memex/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Run tests
        run: |
          cd memex
          go test ./...
