name: "shared/upgrade: Test"

on:
  push:
    branches: [main]
    paths:
      - 'shared/upgrade/**'
  pull_request:
    branches: [main]
    paths:
      - 'shared/upgrade/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: shared/upgrade/go.mod
          cache-dependency-path: shared/upgrade/go.mod

      - name: Run tests
        run: |
          cd shared/upgrade
          go test ./...
