name: "redit: Test"

on:
  push:
    branches: [main]
    paths:
      - 'redit/**'
      - 'shared/upgrade/**'
  pull_request:
    branches: [main]
    paths:
      - 'redit/**'
      - 'shared/upgrade/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.21'
          cache-dependency-path: redit/go.sum

      - name: Run tests
        run: |
          cd redit
          go test ./...
